# ===================================================================
# PLAYWRIGHT E2E TODO (visible in IDE - progress tracked here)
# - [IN PROGRESS] 1) Analyze Playwright HTML report & traces (identify root causes)
# - [PENDING]     2) Apply targeted test fixes (selectors / waits / login flow)
# - [PENDING]     3) Re-run filtered E2E (chromium) for 대시보드|과제 트래커|AI 검색
# - [PENDING]     4) If still flaky: switch global-setup to API-based storageState generation
# - [PENDING]     5) Finalize commits and prepare PR (lockfile/docs if needed)
#
# Notes:
# - This block is intentionally in .gitignore to keep it visible in the editor without
#   creating a new repository file. Update the checkboxes as tasks complete.
# ===================================================================

# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build outputs
dist/
build/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs/
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# Dependency directories
jspm_packages/

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# Vite cache
.vite/

# TypeScript cache
*.tsbuildinfo

# Temporary folders
tmp/
temp/

# Replit specific
.replit
replit.nix

# Test reports
playwright-report/
test-results/
.env.test

# Playwright / test session state (contains tokens, cookies, sensitive data)
tests/storageState.json
