/**
 * MOVED  this file has been relocated to server/tests/integration/search-fts.test.ts
 *
 * Integration tests now live under server/tests/integration/ and are run via:
 *   npm run test:integration
 *
 * This file is intentionally empty and excluded from both the unit and integration
 * Vitest configs (see vitest.config.ts and vitest.integration.config.ts).
 */

// No tests here  see server/tests/integration/search-fts.test.ts
