# Environment Configuration
NODE_ENV=development
PORT=5001

# Database Configuration (Optional - uses in-memory storage by default)
# DATABASE_URL=postgresql://username:password@localhost:5432/wiki_db

# AI Integration (Optional - for content suggestions)
# OPENAI_API_KEY=your_openai_api_key_here

# Replit specific (automatically set in Replit environment)
# REPL_ID=
# REPLIT_DOMAINS=