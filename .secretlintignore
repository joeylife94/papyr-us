# secretlint ignore file — same syntax as .gitignore
# Build and generated outputs
dist/
build/
coverage/
node_modules/

# Runtime environment files — gitignored, not committed to source control
.env
.env.*

# Env example files contain placeholder patterns, not real secrets
.env.example
.env.test.example

# Docker / infra config uses example DB URLs for local dev — not real secrets
docker-compose.yml
docker-compose.test.yml
render.yaml
Dockerfile

# CI workflow files — contain example/test PostgreSQL URLs for CI services
.github/

# Test runner scripts — contain hardcoded test-only DB connection strings (not real secrets)
scripts/run-layer4.mjs
scripts/run-e2e-layer5.mjs
scripts/run-visual-layer6.mjs

# Standalone utility scripts with hardcoded dev connection strings
server/create-comments-table.js

# Test fixtures and mock tokens are intentionally fake
tests/
server/tests/

# Documentation may reference example credential patterns
docs/
README.md
CHANGELOG.md
CONTRIBUTING.md

# Database migration SQL files
drizzle/
migrations/

# Playwright reports and snapshots
playwright-report/
test-results/

# Package lock — no user secrets
package-lock.json
